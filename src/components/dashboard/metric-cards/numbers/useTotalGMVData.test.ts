import { describe, it, expect } from 'vitest'

describe('getTotalGMV query', () => {
	it('fetches the total GMV query correctly and returns the expected data object', async() => {
		const response  = await 
		expect("", ).toBe()
	}
})